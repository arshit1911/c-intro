// program to determine wether a number is divisible by 97 or not

int num;
{
    printf("enter the number\n");
    scanf("%d", &num);

    printf("the divisiblity test returns:%d", num % 97);
    return 0;
}